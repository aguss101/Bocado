<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Galería: Android 13+ usa READ_MEDIA_IMAGES, versiones anteriores READ_EXTERNAL_STORAGE -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bocado" >
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Bocado.Splash"
            android:exported="true"
            android:launchMode="singleTop" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep links custom scheme: bocado://perfil/{slug} y bocado://receta/{slug} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="bocado" android:host="perfil" />
                <data android:scheme="bocado" android:host="receta" />
                <data android:scheme="bocado" android:host="verificar-correo" />
            </intent-filter>
            <!-- App Links: https://links.bocado.tech/{perfil|receta|verificar-correo}/{slug} (autoVerify con assetlinks.json) -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="links.bocado.tech" android:pathPrefix="/perfil" />
                <data android:scheme="https" android:host="links.bocado.tech" android:pathPrefix="/receta" />
                <data android:scheme="https" android:host="links.bocado.tech" android:pathPrefix="/verificar-correo" />
            </intent-filter>
        </activity>
    </application>

</manifest>